# Mitigate intermittent ERR_INVALID_THIS from pnpm's undici fetch against the
# npm registry (esp. on Vercel / Node 22+). Retries + lower concurrency make the
# race far rarer, without relying on any Node flag.
fetch-retries=5
fetch-retry-factor=2
fetch-retry-mintimeout=10000
network-concurrency=8
